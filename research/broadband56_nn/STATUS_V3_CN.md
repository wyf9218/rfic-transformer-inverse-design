# BB00–BB06 七组 10K 研究：公开交接状态

状态快照：2026-09-07 23:06 UTC。本文是脱敏工程交接，不是正式10K精度报告。

## 已完成与未完成

| 项目 | 实际状态 |
|---|---|
| BB00 历史256×3 Tandem MLP原件包 | 私有包已整理，独立进程加载成功；原权重不在本仓库 |
| BB00 新数据训练实现 | 正向/反向、保存、加载、正常续训及隔离诊断续训已实现 |
| BB00 统一入口合成训练 | 24个合成几何，正向/反向各32次CPU更新通过；不是新10K科研结果 |
| BB01–BB06 | 六组架构/训练/评价/打包实现保留，不推倒重做 |
| 七组统一入口 | 数据门槛、只读传输、冻结、串行训练、恢复、评价、打包已实现 |
| 源工作树针对性回归 | 210项通过；软件测试不等于模型准确率 |
| 最近只读生产提交检查 | 5987个正式accepted唯一几何；不是候选数，也不是560000条记录 |
| 正式新10K快照与七组训练 | 尚未开始，WAITING_FOR_10K；没有正式新10K权重或统计表 |
| 后台自动触发 | NOT_INSTALLED；只交付前台单次入口，没有AI周期轮询 |
| 生成几何的真实EMX验证 | NOT_RUN；不宣称物理精度冠军 |

历史参考ID：`real10k_center15ghz_seed20260711`。已核验结构为
10→256→256→256→4 和4→256→256→256→10、tanh近似GELU、独立sigmoid映射。
历史原训练器字节身份仍未证明，旧optimizer状态缺失，因此不宣称历史训练逐步复现。
新数据BB00使用明确记载的新优化配置；历史reference与new10K retrain分开保存，仅算一组。

旧5K试验与历史模型的权重、数据、结果均保留在私有研究存储中。不得把它们
作为新10K默认初始化，或把旧5K结果改称新10K结果。真实数据不随GitHub/worktree复制。

## 后续执行者必须保持的合同

1. 生产任务归另一任务；本研究入口唯一负责七组试验。不得重启生产、改变NN禁用配置、生成新数据或干扰GUI。
2. 以正式提交闭包达到至少10000唯一几何为门槛，固定accepted_sequence1..10000。
   保留每几何完整56点、S/Z及原始有效性标记；缺失或冲突失败，不挑“容易”的数据补位。
3. 七组共用固定几何分组与新train-only归一化/评价协议，所有主比较forward和inverse从随机初始化训练。
   报告实际train/val/test和有效15GHz计数，不能将10K全部称为梯度训练量。
4. 七组共同输入只有15GHz的[Lp,Ls,Qmin,|K|]，不能给六组宽带网络隐藏的完整参考谱或真几何。
   另做六组宽带表；BB00宽带NOT_SUPPORTED，不能伪造输出或给零分。
5. 正向与反向分别统计，使用同一独立train-only FREF；它不进入inverse梯度。
   own-forward结果只是单列诊断，不比较不同定义训练loss。失败保留在预冻结分母。
6. 本轮监督方式不同，是方法/系统对照，不是严格等监督的纯架构因果消融。
   没有生成几何的fresh EMX，始终REAL_EMX_VALIDATION=NOT_RUN。
7. 固定study request、源文件SHA及control_root；资源不足等待、串行执行、恢复已有阶段，不重复提交。
   不用AI反复查询进度。需要后台调度必须另有真实安装/运行证据。

## 代码入口与证据定位

- 操作说明：[SEVEN_MODEL_V3.md](SEVEN_MODEL_V3.md)。
- 单次入口：`python -m research.broadband56_nn.seven_suite check-and-run-once`。
- 阶段命令：`prepare-10k / train-seven / resume-seven / evaluate-seven / package-seven`。
- BB00：`bb00.py / baseline_package.py / bb00_delivery.py`。
- 快照与只读数据：`snapshot_10k.py / snapshot_parity.py / research_data_access.py`。
- 公共评价与结果复用：`seven_evaluation.py / reuse_evaluation.py`。
- 去重锁/日志：`study_once.py`；原六组实现见本目录README。

已验证的真实私有一条启动命令、数据访问配置、来源交接、逐文件SHA索引、
历史包和测试日志保存在项目私有报告区，不在公开仓库暴露路径或凭据。
克隆仓库后必须自行提供有权访问且已核验的私有输入；没有这些输入只能运行合成测试，
不能声称能够直接训练真实10K。

私有准备收据SHA256：`98db042a74aaeaba1ec7f0b55ff9e02175da1e9e23ca2276d815094c181a9fef`。
源工作树最终测试XML SHA256：`f4a9f65cad5a763ad684ea509119c5bf3f58f90a80185adb8ed7385b9e3078e1`。
真实单次等待入口验收SHA256：`9c707216340db518128f5f1252443fbe51f3706a901fdd44285b46fde87aef2f`。

本公开分支只提取研究代码和合成测试，不包括另一任务尚未发布的生产改动。
不含私有数据、权重、工艺资产或个人站点路径。它不解决仓库既有历史的发布治理问题，
也不表示本机/MARS的原研究环境已改换到这个公开分支。
