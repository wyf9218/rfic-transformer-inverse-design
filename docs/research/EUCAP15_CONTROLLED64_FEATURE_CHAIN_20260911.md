# Frozen controlled64: feature-chain reader

Status: **SCOPED_SYNTHETIC_TESTS_PASS; NOT_NATIVE_DEPLOYED**.
This is a code increment, not a new physical experiment or a production
qualification receipt. It preserves the already-frozen64 candidates and does
not regenerate targets, Q choices, geometry, data or model weights.

## Implemented

`research/broadband56_nn/eucap15_controlled_evidence.py` uses the actual
controlled64 schemas and the existing read-only mirror, geometry/GDS/Calibre
checks and original56-point label verifier. It binds the frozen candidate,
original and resolved input pins,14 runtime modules, wrapper and process,
same GDS, exact output command, S4P and feature receipt.

Its public functions are `load_context(reader, intent_pin, manifest_pin)` and
`inspect_feature_chain(reader, entry, batch)`. The batch must be produced by
the frozen-context loader; it is not a caller-authored authorization.
No simulator, model inference, training or network operation is invoked.

The reader deliberately returns:
- `terminal_publication_verified=false`;
- `solver_start_verified=false`;
- `production_admission=false`.

A verified feature chain alone does not establish a closed owner export,
actual solver start order, budget consumption or official accepted membership.
The existing metadata-only context preflight was already completed and was
not rerun for this increment. There is no installed automatic physical
consumer, new owner loop or deployment command in this code increment.

## New directed tests

Only `tests/test_eucap15_controlled_evidence.py` was executed:
**83 distinct synthetic cases passed in2.45s, one execution, zero failures**.
Old suites were not rerun. Exact tested implementation SHA-256:
`7f60518fff103c7251aec2e259e02473ee917e9f47c92e8e5944bda40c0b7673`.

The fixtures exercise candidate sources, pinned path mapping, analytical
holds, original Q/model/geometry identities, strict-invalid labels, runtime
source closure, GDS/DRC evidence and solver-command mismatch rejection.
They use opaque synthetic GDS/S4P bytes, not real layouts or EM responses.
The schema tag `FRESH_REAL_EMX` in a fixture is not a real-EMX result.

Author receipt SHA-256:
`fd8430d209a1691e2544776889703b05696ebd7812f0b105706ab0441260563b`.
Its exact commands, source snapshots, JUnit and original output are retained
under the private evidence root
`reports/eucap15ghz_20260908T220300Z/controlled64_feature_chain_20260911_v1/tests_v1`.
A pre-test source that lacked runtime closure is preserved separately;
it was not used for the83 passing tests.

Independent static review accepted this precise runtime-binding repair:
`7c05a1ef3c5229e34caacb2f3e68c7b9b59c4002291f7ff4e4b8b30210aa42a7`.
It read the actual native interface and reused the author test receipt,
without rerunning tests. Its acceptance is limited to source consistency,
not native execution, physical success or deployment.

## Native-side incremental interface

The sole native owner also supplied a standalone RESULT writer:
`controlled_result.py`, SHA-256
`212fbec34d8a193034424df348e3c055def3b812fe73d79328accc5ef290062a`.
Its current16 directed synthetic tests passed; earlier14 are not added to16.
It retains failures and original64 membership and requires pinned birth,
plan, release and actual executable evidence for a recorded fresh result.
The writer is **not integrated with the owner and is not deployed**.
The owner subsequently supplied a bounded closed-metadata capture component
(12 new directed tests passed), SHA-256
`de7bc877c73105262757b2ec785f706f31806ec7d5dea329dd7283efd9f06353`.
It preserves unknown records and does not reconstruct actual start order or
transport physical artifacts. No real capture was executed. This research
reader does not yet consume either the RESULT or the closed capture.

## Actual experiment state is not refreshed

No new native terminal, native start, model update or certified production
submission was produced by these local components. The original
000125/Q10 and000126/Q15 remain unobserved; their current state is UNKNOWN.
The last native SSH attempt at2026-09-11T00:40:39Z exited255 with
`Connection closed by UNKNOWN port65535`. No repeated SSH was attempted.

Current MARS disk, quota, allocated bytes per certified sample and total
certified count toward100K remain UNKNOWN. A saved wrapper pin is not the
native executable identity. Real128 closure, live leases/resources, native
binary binding, six-hour admission origin and2GiB incremental reservation
remain required for the sole owner's deployment.

The original64 budget is unchanged: at most16 actual native EMX starts per
arm,32 total,6hours,2GiB and at most2 concurrent after live admission.
Equal-solve and first4 qualified-train comparisons remain separate and
NOT_RUN. Neither P215 row totals nor31 research members are promoted to
production accepted. No paper figure or numerical result was fabricated.
