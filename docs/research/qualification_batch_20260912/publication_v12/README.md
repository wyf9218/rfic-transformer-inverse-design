# Increment M12 — actual EMX production and endpoint v2

Observed 2026-09-12 08:31:50 UTC; baseline08:21:28 UTC.

- Requested48 / executor48 / persisted active permits6 / actual nativeEMX6.
- New24 terminal:20 completed/extracted EMX,3 candidate failures,1 analytic not dispatched.
- New7 distinct strict/in-range geometries; all7 formally admitted with their original splits (5train/2test).
- Fresh formal+12 =7 new-window +5 previously received eligible results. Historical formal+215.
- Certified union lower bound5664. This is not5664 gradient-training samples or a complete100K pool.
- Original256 cumulative75terminal=50fresh+14candidate failures+11analytic;181 without terminal are not181running.
- No old physical result, candidate, budget, split, model or failure was overwritten or repeated.

## Actual capacity bottleneck

Persisted non-atomic stage evidence has57 waitingCadence,1 waitingCalibre,6activeEMX,
and no other EMX-ready candidates. Current upstream remains1/1; unchanged resource
policy replay permitted34 additionalEMX but issued no permit itself. Thus this cut
shows upstream supply shortage, not the formerCPU-WAIT. Ordinary automatic
successor8/8/48 is installed; native endpoint-v2 integration was handed to the sole
owner and is not claimed installed or physically validated in this freeze.

## Minimal endpoint fix

The six prior Cadence failures reproduce on the exact active source/config.
Canonicalization moved the P002/P004 terminal face cross-center by+/-0.0075um
relative to nominal metadata. New explicit shared_port_edges_lineage_20260912_v2
binds the same pre/post polygon edge and preserves the exact-width/axis/label guards.
Six retained captures and3 new negative guards pass; one actual export wiring case
passes. No polygon/endpoint/label mutation, native tool invocation or GDS output
was used as this development test. The original six failures remain failures.
The v2 module adds a stricter width check exposed by a retained negative-test FAIL.
Legacy/default/v1 behavior stays unchanged. Exact source pins and a transparent
label-only hash transition are recorded; this is not fresh physical validation.

SNAPSHOT.json and RECEIVED_NEW24.json are byte-identical to private received artifacts.
REPLAY_SUMMARY.json is a compact projection, not the original replay receipt.
Private process/config/PDK artifacts are referenced by path/hash, not repackaged.
The saved check scripts require their original private inputs; no portability or
standalone full-suite execution is claimed. Full100K and final independent10K remain incomplete.
This short observation window is not a stable hourly throughput benchmark.
