# M9: actual partial-admission diagnosis and formal4700 observation

Native observation:2026-09-12 07:31:08UTC; kernel constraints observed separately07:25:33UTC.

Requested/admitted/executor/actualEMX=48/0/48/0. The existing dynamic limits support partial concurrency; there is no requirement to fill all48 at once. Actual idleCPU2.430 is already below the existing4CPU reserve, before requesting a2CPU solver. Load1/5 also fails the project's1.10 thresholds. Other resource checks pass, and300EMX licenses are free.

The observed process affinity/cpuset is0..191 and inspected cgroup CPU quotas are unlimited, with no throttling. This does not prove exclusive CPU entitlement or unlimited school administrative permission. The1.10 number is an engineering configuration, not an independently proven school rule. No guard/healthy runtime was changed, and no benchmark/oldQA/native experiment was rerun.

Window07:16:02→07:31:08: newEMX0, newstrict/range unique0, freshformal0, historicalformal+236. Formal observed union4700; only new4465..4700 inspected. This is not4700fresh simulations, current training rows, or a complete100K audit. The ordinary owner and installed continuation are alive; no successor launch observed. The original256budget and235without-terminal candidates remain unchanged.

[Snapshot](SNAPSHOT_M9.json) keeps exact timestamps, head/source identities and resource values. [Native constraint evidence](NATIVE_CONSTRAINTS.json) distinguishes actual deployed5b23892runtime from the earlier ab763b0candidate; their observed difference is only the pre-existing ensure_plan line, not resource logic.

The previous P215 first100 and TRAIN002 are complete and must not be repeated. The native snapshot preserves its07:31cutoff. A separate07:36:50actual reception subsequently verified3existing fresh RESULT/FEATURE interfaces and2formal records: DOE008 is original test, TRAIN002 original validation, and DOE009 fails half-SRF and K range. Added qualified train0 and training-coverage gain0; no target/proxy errors are defined. These are delayed receipts, not3new solves or2new formal commits in the observation window. [Actual received rows](LAST3_RESULTS.csv) preserve physical values and splits.

The distinct training-source prefix100 has100unique raw identities and100/100 exact geometry/evaluation/S4P joins to the existing SQLite identities. Subsequent actual work completed100GDS necessary checks (allPASS) and100original111-point response re-extractions (allstrict under the current mapping;93inside the full Lp/Ls/K range andQ10–20). The7outside cases all haveK<0.2 and were not clipped. These differ materially from the previous base-source100, which all failed current GDS grid checks. Source-order prefixes are not representative samples; do not extrapolate93/100 to118218 or100K.

Current formal qualification remains UNPROVEN and newly committed0 for this source. Existing Calibre/current process-and-port/execution binding and split/union closure are incomplete; necessary gdstk checks are not Calibre. Full source/database hashes were not freshly recertified. [Actual100 values](TRAINING_SOURCE100_VALUES.csv) and [qualification status, input identities and exact executed commands](TRAINING_SOURCE100_STATUS.json) preserve the distinction. New broad111 rows remain in the private pinned CSV. Exclusive classification across only the two disjoint inspected100-row partitions isA0/B100/C100/D0, with215585other originalP215 rows outside this audit scope.

Suggested author-verified table caption: “Two sequential100-row historical source partitions differ in current-contract readiness. The base-source partition fails the required geometry grid; the training-source partition passes necessary grid/edge checks and has93conditional numerical strict/range cases. No formal current-contract admission or sampling advantage is established by these checks.” No AI figure was generated; no model or sampler was updated from these data.

[Scoped earlier reception summary](LAST3_AND_SOURCE_SUMMARY.json) retains its07:36timestamp; [receiver](receive_increment.py) does not reconsume the prior6 results. The new explicit100 runner reuses the exact111 extraction core and changes only the input scope/caveat metadata plus ordinal uniqueness check; previous0..99 results are untouched. The initial large JSON display reduction exceeded tool output limits before any GDS run; the saved small adapter then performed the new100 successfully. Publication does not gate production.
