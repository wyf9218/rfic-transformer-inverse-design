# Actual qualification and native recovery, 12 September 2026

This dated increment follows commit bb615e19d913003c318775504986a20fdbfa736c.
The earlier SNAPSHOT_0325.json and its manifest remain unchanged and describe that commit,
not the current contents of source paths after subsequent fixes.

## Formal certification actually published

The native owner appended the six original controlled64 core members, then all304
qualified members from the completed first1000 audit. The ledger read back341 unique
geometries:209 train,66 validation and66 test. All three geometry identity namespaces
are unique; the original37 prefix is unchanged. Historical304 preserve184/63/57 splits.
Source audit classifications remain304 qualified,220 out of range and476 physically
invalid; duplicate/missing/incompatible are zero only for those1000 source records.

These are historical qualification submissions, not new simulations or newly added
training examples. Do not add341 to old21135 or6700. The complete100K qualified count
is still unknown. The current bounded formally certified lower bound is341.

See SNAPSHOT_0402.json for exact counts, scope, timestamps and receipt hashes.
Readback was a metadata/pin/source check; it did not rerun physical QA or source joins.
The next1001..2000 audit and publication are separate work, not silently included here.

## Real execution, not preparation alone

The two-source inner Cadence policy propagation fix was deployed with the existing
six-source recovery binding. The unchanged strict output consumer remains in use.
Three new nested-policy regression tests passed; old passing suites were not rerun.
The first recovered candidate passed actual GDS auditing/Calibre and produced fresh
EMX plus original5–60GHz56-point evidence. This establishes pipeline execution,
not an improvement in physical accuracy or a strict-qualified geometry.

At04:02:46 UTC the existing owner PID2776362/start398895226 was alive with its three
locks. Two actual native births and two fresh results were observed; one original
analytic failure was not dispatched. These are3 closed proposal states, not3 solves.
The other253 have no closed result in this snapshot; that is not253 field-pending jobs.

Both completed candidates are core-ineligible. For the first, descriptor/physics QA
passes but below_half_srf is false: primary SRF is estimated at29.44922769360639GHz.
Its response is numerically in the Lp/Ls/K and Q10..20 ranges; that does not override
strict validity. No candidate replacement or gate relaxation is permitted.
The second's complete artifact transport is not yet available to the research consumer;
its current status comes from the actual owner RESULT and birth metadata.

The unchanged original window is03:30:49.723123–15:30:49.723123 UTC, cap256 actual starts
and5GiB cumulative budget-root allocation, with stricter1solver×2CPU admission.
All recovery outputs count toward that same budget; the old controlled64 budget stays closed.
Software-failed earlier attempts remain in their original directories and cost history.

## Storage and interpretation

Mount available bytes at the capture are493760962560. Research quota is unknown.
Changes in shared mount availability are not attributable task storage changes.
Zero new qualified samples means per-qualified storage is undefined, not zero.
These192DOE+64train-neighborhood proposals contain no target/proxy/q_proxy:
do not invent target error, joint hit, inverse-design accuracy or Q-scan benefit.

The scientific rules and model/test identities are unchanged. No new training,
test tuning, old target generation, cleanup, repeated QA or AI scientific figures occurred.
[Executed publication source](publication_v2/SOURCE_MAP.md) preserves exact module identities.
[Inner-policy fix](../EUCAP15_NESTED_CADENCE_POLICY_FIX_20260912.md) records the actual defect
and the limits of previous outer-only tests.
