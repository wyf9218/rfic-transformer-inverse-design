# Incremental qualified-ledger publication candidate

Status: local synthetic tests passed; **not deployed**. No physical results or formal records were generated by this work.

In the next safe new metadata process, retain the original 885f794a historical module and its existing admission/atomic/geometry imports. Instantiate once, not once per candidate:

```python
from incremental_history import IncrementalPublisher
publisher = IncrementalPublisher(actual_qualified15_root)
result = publisher.publish(evidence, utc, reader=existing_reader)
```

`actual_qualified15_root` is the existing `qualified15_single_member_v1`, not its `records` child. The first-record SHA defaults to the original admission authority. Keep the current ordinary BusyStudy retry callback and sole-writer coordination. A busy lease propagates unchanged; this module does not start another worker/controller or silently retry.

The unchanged original publication function is bound to a private globals dictionary with only its `ledger` dependency replaced. Existing evidence reconstruction, lease, append, and readback still execute. Global `history_publication.ledger` is not changed. The cached ledger's compact internal entries are sufficient for that exact pinned function; they are not a replacement public full-record API.

Every new record receives the original complete record/evidence, mixed-schema, prior/checkpoint, request and three-namespace checks. Every cached prefix path receives metadata identity verification. Source-file or directory/lock identity change, lost prefix files, or changed prefix contents/metadata fail closed. No persisted cache is trusted across restarts.

This removes repeat record-body JSON reads and hashes, but retains O(N) metadata and the original duplicate/replay loop. `summary().record_body_reads` measures only the cache validator's body reads; original evidence/readback reads remain. Ordinary files—even exclusively published ones—do not provide a trustworthy immutable-prefix guarantee, so omitting all prefix metadata checks would weaken protection.

Three new synthetic fixtures passed once. No existing physical audit or old test suite was rerun. The unique native owner decides the supported deployment boundary and measures actual effect; do not call this deployed production or a demonstrated throughput gain.
