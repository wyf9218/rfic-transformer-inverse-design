# M4：资格、生产接收与端点诊断增量

本文件冻结于主任务确认的04:59:15 UTC现场切点；首293独审05:01完成，仅核验此前入账。M3与原始实验不改写。

| 项目 | 已核验结果 | 边界 |
|---|---:|---|
| 完整运输的正式读回 | 943：train 573 / validation 185 / test 185 | 04:58:17 UTC：650→943，新293、0HOLD；不是完整100K |
| 后续现场账本观察 | 986 | 04:59:15 UTC；额外43条尚未完成逐记录增量审查，不推算986的split |
| 历史来源审计 | 21,135 条：合格 6,329、范围外 4,705、物理无效 10,101 | 仅该八来源；重复/缺证/不兼容各0，不外推全部P215 |
| 详细读回切点的待提交量 | 5,425 条 | 对应943切点；986现场观察后余量不混写为同一切点 |
| 新256实际接收 | 6 fresh + 1 原解析不派发 + 3失败终态元数据 | 6 fresh中2个strict且范围内，均已正式入账 |
| 两合格点覆盖 | 新增格0、欠填改善0、高K/高Q增量0 | 3801与3821训练基线仍159/163格；只增加已有格密度 |

原尾部162条已由已有56点证据准确取出15 GHz行并绑定第八来源；未插值、未重新EMX。
审计合格6,329不能当成本轮新训练行，也不能替代943详细读回或986现场观察的不同证据层级。
首批追加保留原split：新增175 train / 60 validation / 58 test，旧650前缀不变。
当前开发模型、split、训练支持域及全部旧实验不随资格提交自动改变。

## 新失败必须分开解释

- DOE006：布局导出前P004端面识别失败；不是DRC/SRF标签无效。
- TRAIN_NEIGHBORHOOD-001与新增DOE007：原EMX包装阶段因swap准入返回RESOURCE_WAIT，各失败hook未启动native。
- 上述3个原失败对象保留；包装失败不能改写成真实求解器物理失败，也不推断所有历史尝试的启动数。
- DOE007首次接收因既有模块导入路径失败，run_v2失败保留；新run_v3只增量接收该例，未改runtime或重复消费旧结果。
- 所有DOE/邻域proposal没有目标或q_proxy，故不定义目标误差或联合命中率。

## 实测成本：只使用04:29:47 UTC的六求解器窗口

六个真实求解器Wall-clock合计593.66秒、CPU合计1201.79秒，包含4个非core结果成本。
原准入至该观察共3538.233995秒，得到2个source-core：早期2.0349个/小时。
研究预算根实际占用88,547,328字节，早期摊销42.2227 MiB/source-core。
这不是单样本净保留量或稳定产能；配额、最终100K耗时/存储预测仍UNKNOWN。
该窗口尚无正式append，不把后来的649/650入账倒填为当时吞吐分子。

## DOE006的可检查修复草案

唯一实际案例纯内存重建证明：沿边长逐段网格化把现有P004全宽端面横向移了−7.5 nm，
而旧匹配器仍使用名义中心，因此漏判。实际轴向端点、宽度及10 µm overlap正确。
草案仅把该端口元数据绑定回同一raw→canonical edge；不扩大旧公差。
8个端口原构造检查通过，polygon/端点/label零修改，5个针对性负向fixture拒绝。
原失败不改写；这不是历史保存的polygon，也没有GDS/DRC/EMX验证。部署NOT_INSTALLED。

新history publisher支持已执行cursor的原source序列与独立资格序列；8项新测试首跑通过。
该精确版本已由原生owner部署并正式追加首293，完整读回与293逐记录增量独审均已通过；旧650未重读。
首条重放added0来自已执行wrapper断言；独立FIRST_READBACK未运输，不宣称复查者重新执行了重放。
同一publisher的3001..21135普通后台接续已实际启动：PID1245823/start399304935、PPID1、独立RUN.lock。
现场已闭合3001..3100块（新增28/0HOLD），终态尚无；不推算未来完成数，也不是第二个原生控制器。
公版拷贝本身不意味着可脱离私有绑定运行。
04:59:15最新现场已覆盖旧CPU等待：原owner2776362仍在，DOE008真实EMX PID1224211/start399301459正在R。
实际并发1、每job2CPU；该在飞案例尚未计为fresh完成。磁盘可用493,967,306,752字节，不是配额证明。
资源等待修复已有独立静态GO，确认仅匹配两种精确异常拼写；保存的7项测试使用mock与synthetic资源。
资源阈值、预算、deadline不变；该GO不代表现场资源准入或原生部署，旧失败不会自动重开。

切点后的持续生产授权已接单；自动资格入库与successor部署尚未证实，不当作04:59任务已完成。
该授权原文仅以私有handoff来源pin列于manifest；不伪造原消息时间/id，不倒填05:02后的新物理结果。
精确源SHA见SNAPSHOT_M4.json、publication_v4/SOURCE_MAP.md和M4_MANIFEST.json。
本发布整理零新训练/测试/仿真/账本写入；完整P215与100K合格总数仍UNKNOWN。
