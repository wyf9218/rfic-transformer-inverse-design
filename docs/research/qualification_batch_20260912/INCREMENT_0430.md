# M3: 649 formally certified members; research and owner cutoffs remain separate

The formal15GHz union is now **649 unique geometries:397train,125validation,127test**. The native owner first appended307 historical members, reaching648 while preserving the previous341-record prefix and every split. At04:31:05UTC, one new256 train member was actually appended as649; replay added0 and the648-record prefix was unchanged. The separate incremental research readback passed without redoing physical QA.

## Historical certification

| Source rows | Processed | Qualified and formally added | Out of range | Physically invalid | Duplicate / missing / incompatible |
| --- | ---: | ---: | ---: | ---: | --- |
| 1–1000 | 1000 | 304 | 220 | 476 | 0 / 0 / 0 |
| 1001–2000 | 1000 | 307 | 225 | 468 | 0 / 0 / 0 |
| Combined | 2000 | 611 | 445 | 944 | 0 / 0 / 0 |

The ledger union is the prior37 members plus611 historical qualified members plus1new256 member. This is a reliable bounded lower bound, not full P215 qualification or the100K target completed. Source rows2001+ are **NOT_INSTALLED** as the next executable partition. Old broadband acceptance is not incremented again.

The current frozen source/input and15GHz-label binding covers20,973 rows across seven source partitions, not21,135. After the2,000 processed rows,18,973 remain source/label-bound but not yet qualified. The tail20974–21135 comprises162 rows needing frozen label or eighth-source binding; it is not an already-ready queue. Binding alone does not certify physical qualification.

## New256 production and research reception

The saved04:29:47UTC owner transport and completed incremental research reception now contain **6fresh results and1original analytic failure**. Of five DOE results, one is strict/core eligible; the one train-neighborhood result is also eligible and was formally committed as649. The eligible DOE004 retains its original train split and is not yet formally appended in this snapshot. New004/005 reception added two records without re-consuming the earlier closed physical cases; the earlier four-result cache snapshot remains unchanged.

The ownerPID2776362 was observed in that capture; this is not current liveness. Remaining unobserved proposals are not declared pending, and a subsequent wrapper process is not evidence of another native start. The original256-start/12-hour/5GiB budget and one-solver×2CPU admission are unchanged.

Targets, proxy targets and q_proxy are null for these geometry proposals. No target-error or joint-hit rate is defined. Callback compatibility review passed without a patch or rerun; the unique native owner used it for the actual first append. The eight new synthetic tests had passed on their first run and were not repeated for publication. This does not establish an installed automatic callback for every future candidate.

The first train-neighborhood result lands in actual EM cell(5,4,0). Both retained training baselines already contain86 members there, potentially87 with this member:0new occupied cells,0underfilled-cell improvement and0high-K gain. Their159/163 occupied-cell totals do not increase. This descriptive source-core coverage receipt retains its original pending-formal status; later ledger admission does not automatically change the model, training pool or frozen coverage files.

## Cost: a different, earlier observation window

The04:12:13UTC cost capture contains **three** completed solvers, not the later four- or six-result population. Their native logs report251.49s summed wall time and504.72s summed CPU time, retaining two physically invalid solves.

From original admission to that cutoff,2483.34643s elapsed and one source-core result was available: approximately1.45source-core geometries/hour. The entire74,047,488-byte budget root divided by that one result is70.62MiB, including early software failures, recovery, logs and in-flight work. Neither number is a steady-state estimate, a formally accepted rate or a100K forecast. Quota and single-retained-sample cost remain unknown.

[Machine-readable snapshot](SNAPSHOT_0430.json) preserves separate evidence cutoffs and receipt hashes. [Byte-identical code](publication_v3/SOURCE_MAP.md) supplies the actual historical publisher and read-only receiver. No training, old tests, physical QA, cleanup or paper repackaging was repeated.

The307-record readback command completed its assertions, but its full stdout exceeded the tool output limit. Its compact receipt explicitly records that limitation; a retained full stdout or repeated whole-ledger check is not claimed.
