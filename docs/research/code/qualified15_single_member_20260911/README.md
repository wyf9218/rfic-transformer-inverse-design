# Exact-case data publication adapter

Review copies of the two files used to publish one already-qualified DOE006 member.
This is not a generic future-data admission service, simulator, or replacement production controller.
No private evidence JSON, geometry values, PDK, credentials or deployment command is distributed here.

- `adapter.py` validates the bound member, original physical QA, current source identity and known-pool disposition; it preserves the historical candidate and geometry IDs separately.
- `atomic_primitives.py` reuses the existing permanent-inode lease and fsync/immutable-publication functions.
- The owner executed one publication and one idempotent no-op replay on MARS; new increment sequence1, old broadband sequence null.
- Nine publisher tests passed after correcting only the temporary macOS symlink fixture; the first fixture failure remains in the private evidence.
- Inputs and deployment are private and immutable. Do not execute this copy against another ledger or treat it as approval for additional members.

See [the scoped publication record](../../20260911_first_qualified_increment.json). Full-history qualification and union counts remain unclosed.
