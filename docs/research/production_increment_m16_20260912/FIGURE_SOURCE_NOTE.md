# Figure-source increment: prior received25 and delayed formal5

Use `ACTUAL_LANDINGS.json` for author-made traditional plots. No figure was generated.

This is the first offline projection of the already received25-terminal cohort (24 real EMX completions,10 strict/in-range and formally accepted), not a new simulation. The10 formal members retain8train/1validation/1test. Five earlier results newly admitted at that earlier source cut remain a separate backfill cohort:4train/1validation.

Relative to the unchanged3801-train,512-cell reference,8 new formal TRAIN members occupy7 distinct cells. DOE141 falls in cell[4,1,2], which contained0 members in that frozen reference; its actual[Lp_nH,Ls_nH,Qmin,K_abs] is[1.4365479856548058,0.8590769759376855,12.62144065307214,0.3855131499929888], formal record006445. This is not proof that the current whole pool gained a previously empty cell: other post-baseline cohorts are outside this calculation. No159→160 cumulative claim is made.

The4 delayed-formal TRAIN members occupy4 already-filled reference cells. Validation/test/failures remain visible and do not change TRAIN coverage. All12 included TRAIN rows have actual Qmin within10..20; none has|k|>0.8. No claim of high-K coverage or sampler superiority follows.

Suggested caption: Separately counted real-EM response landings of newly completed and delayed-formal TRAIN members against one frozen reference. A newly observed baseline-empty cell is descriptive and does not establish whole-pool coverage gain or a causal sampling advantage.
