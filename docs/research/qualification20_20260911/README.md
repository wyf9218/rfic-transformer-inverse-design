# Twenty formally committed 15 GHz qualified geometries

At 2026-09-11 22:01:56 UTC, the native owner processed the nineteen remaining train members of the original 31-member research increment. All nineteen were qualified, committed and read back; nineteen verification replays added zero records. Together with unchanged sequence 1, this separate immutable ledger contains twenty unique train geometries, sequences 1 through 20.

This is a known-pool certified increment, not a certified total over all historical sources. The old broadband ledger remains 21,135, and no arithmetic addition to 6,700 or 21,135 is justified. Full 100,000-goal qualification is still unclosed. The original three validation and eight test members remain untouched, and the frozen 6,329 model/data view has not changed.

Each admitted geometry satisfies the recorded current process, port, GDS/DRC, real-EMX, half-SRF and 15 GHz range requirements. The ledger references 1,120 original frequency rows, which does not mean all 56 frequencies per geometry are strict-valid. Public rows contain identities and selected response labels; private geometry, PDK and raw artifacts remain in their original locations with source hashes.

No new EMX, training, historical SQL query or repeated physical extraction was performed for this admission. The separate controlled64 trial contributes zero formal admissions in this release. Research disk available space was 392,333,971,456 bytes at the recorded observation; the quota command only identified other mounts, so the research quota remains unknown. No cleanup occurred.

The code directory contains byte-identical publication/admission helpers and their scoped tests for review, not a second native scheduler. Runtime private INPUTS/proofs/contract artifacts are intentionally not copied here. Do not execute these against a new namespace or bypass the unique native owner. Replays must preserve the existing first record and immutable chain. SOURCE_MAP and SUMMARY give exact source identities and the bounded independent-review scope.
