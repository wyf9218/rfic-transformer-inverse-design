# M20 minimal budget-walk consolidation: staged, waiting for safe boundary

Recorded execution cutoff: **2026-09-12T11:25:24.650589Z**. No second ordinary production cut was performed after M20's11:15:44 observation. Its last verified formal count6589 and native15 remain time-stamped historical observations, not newly measured here.

## Minimal code change

- `fixed48_runtime.Manager.require_ready` still checks fault/deadline and `owner.verify_release`, then obtains the actual current `storage_snapshot` once. The exact original `total_allocated_bytes >= 5368709120` condition still raises the original `BudgetEnded` error. The method returns that snapshot.
- `admit_candidate_count`, `candidate`, `acquire` consume that return value rather than immediately repeating a second recursive full-budget-root walk. Each pre-admission pair changes from two scans to one, not all lifecycle/storage scans to one. Existing post-claim and final-release storage publications remain.
- No cached or estimated root allocation replaces a fresh scan. Source identity, post-I/O resource rechecks, all claim accounting, resource limits,5 independent checks,8GiB-per-tool reservation and physical/scientific requirements remain unchanged. M19's permit-release locks are not rewritten.
- Reuse of the existing continuation installer required one small checkpoint composition correction: `replay_waiting_state` starts from an existing `continuation_checkpoint` when present, instead of returning to the original initial owner/index1. The exact current state was replayed as releasebd8ab853..., next_index3, two prior manifests569e1abd... and a3d23e60.... No batch1 or batch000002 regeneration.

## Tests and immutable package

Only **6 new targeted tests** ran, all PASS on local and approved MARS private Python. Runs on two hosts are not counted as12 tests. M19's24-test suite was not rerun.

Tests cover: old two walks versus new one; exactly-at/above cap rejection; all three callers and candidate claims; resource aging during I/O still blocks grant; shared fault blocks before I/O; checkpoint-on-checkpoint replay reproduces the old chain mismatch and preserves next_index3 after correction. Inputs and process identities are explicitly synthetic, not production evidence.

Remote package:
`/volumes/research-localdata/ywang3652/eucap15_native_owner_20260909T062500Z/continuous_budget_walk_20260912T112357738265Z`

- STAGED SHA **4205702f0d7d6ac240603b908defe6539c45ae7a956dec66e65262fef5072fec**.
- TARGETED_TESTS SHA **e507ece7d292e399d2fdb5ac03cd7188cac8bf8c755b9e89eb7c8c1f92275502**.
- New runtime SHA **23eddf702afb6c3f5ee64ac9c415c56376199857dd6fe915f67856a456f8b99f**.
- New continuation SHA **1584fbcf8c377e1d26caf932f1b2442416af18cf15734d3b6562a6f3a13cdbc4**.
- Parent runtime0ad043b6... and parent continuationac3e4033... were read from the actual previous immutable deployment and checked, not inferred from filenames. All other deployed runtime sources were byte-identical.

## Actual boundary-wait launch, not completed runtime deployment

A single finite installer was actually started at11:25:24UTC: **PID516965/start401625264**. Its launcher returned successfully. It has a new session, stdin `/dev/null`, and server-side log; it does not depend on an open Codex tool session. It does not own production leases, run a new resource monitor, or dispatch simulators itself.

- DETACHED_INSTALLER_LAUNCH SHA **3ac4b7721270c6056a72685808ee761cf65b2696e904306fff23907c6c8241e4**.
- BOUNDARY_INSTALLER_START SHA **24abdbbb3b192d26af42467c6458c9b07f19fb5c98084221d5063f80b88f5a6f**.
- The unchanged `install_at_boundary.py`/`quiescent_handoff` still require full batch terminal, exact previous owner death, every standby thread stopped, and **no live descendants or native tools** before replacing the standby. A still-live metadata child must finish; it is not killed or exempted.
- Maximum one-shot wait5400seconds. If no supported safe boundary exists, the old healthy production remains and an explicit timeout/failure is written. No retry or new installer should be launched blindly.

The new runtime is **STAGED / SAFE_BOUNDARY_WAIT** at this receipt. Do not call it deployed to a solver, do not attribute current throughput to it, and do not claim48 native execution. Existing native owner, samples, current batch000002, budgets and metadata continue unchanged.

Next legal entry is the already-running finite installer's `INSTALL_RECEIPT.json` or `INSTALL_FAILURE.json`, then new-release results only. No AI polling of unchanged logs or second production cut is needed merely to wait. Root task has received real pins/source paths and owns central-memory/public-safe Git. No private data/PDK/GDS/credentials were published by this data-owner.

One local apply_patch request initially attempted delete+add against the same file and was rejected before edits or any remote execution. It was corrected to a normal update. This had no production effect and did not trigger an installer attempt.
