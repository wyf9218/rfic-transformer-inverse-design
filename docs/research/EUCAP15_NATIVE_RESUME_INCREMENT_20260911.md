# 15 GHz native-resume work package — verified offline increment

Continues `0699bab965c48b4bc243f757fa300250edf378a1`; not a new study.
The 100K qualified-geometry and final independent10K objectives are unchanged.
This increment does not certify new production members or new EMX outcomes.

## New frozen-proxy finding

The saved preselected candidates of the original development128 were checked
against their own frozen targets and original inclusive absolute tolerances:
Lp/Ls 0.125nH each, Qmin 1, K_abs 0.04000000000000001 (saved binary64 value).
The Q selection, weights, target order and all historical failures are unchanged.

| Original state | Requests | Proxy already exceeds at least one tolerance |
| --- | ---: | ---: |
| Analytic failure | 35 | 24 |
| GDS failure | 6 | 2 |
| EMX strict-invalid | 37 | 23 |
| Strict-valid | 48 | 8 |
| Pending in accepted snapshot | 2 | 1 |
| All original requests | 128 | 58 |

58/128=45.3125% already exceed at least one tolerance before EMX;70/128 pass
the proxy four-feature check. There are no unknown proxy comparisons.
Feature exceedances are nonexclusive: Lp36, Ls27, Qmin22, K_abs54.
Selecting the lowest combined score is not a guarantee of four separate hits.
This is saved-proxy diagnosis, not a new physical accuracy result or a cause.

Per-request K_abs residuals retain signs:
`EMX-target = (proxy-target) + (EMX-proxy)`.
All85 available actual-response rows close numerically;48 strict-valid and37
descriptor-only invalid responses remain separate.43 missing EMX responses
remain null. Absolute components are not causal contribution percentages.
Equal totals alone do not prove equal membership. A new independent per-ID
cross-tab confirms, within these48 strict cases only:40 pass both,8 fail both,
0 proxy-only passes and0 EMX-only passes. This is a descriptive association,
not proof that proxy predictions can replace EMX or explain every failure.
No test target or diagnostic outcome is fed into the frozen model.

Evidence: original result CSV SHA
`d27ce63a60902f00f1f1f53eef2972168d2ef3d349485f6c81c896120f47c75d`;
freeze SHA `b279a1c2785b85777c7cc1ca311e500ac7fa54d017cec19f10afb820ecef3c68`.
New execution receipt SHA
`db47058f2a1279a7a4ca98e769a554db428fa4a32b3d7d037f6fc84cbbcc0985`.
Independent arithmetic/membership review SHA
`960e79c02747fceacebf2ec7796e5df1affccb3e69667e183d8a6108eb516d46`.
Cross-tab with member IDs SHA
`af0c18f5442c5aae36d10cb098b773e9882223d63de38d7ed4bc1fe6254ffb79`.
Public aggregate: `results/eucap15_native_resume_20260911/PROXY_PRESELECTION_SUMMARY.json`.
Private per-request artifacts remain in the recorded local run directory;
the published script is a byte-preserved copy, not a new execution.

## Incremental last2 reader

New `research/broadband56_nn/eucap15_development128_last2.py` verifies a full,
closed owner publication, then checks only newly closed000125/Q10 and000126/Q15
chains while preserving the accepted126 result rows. Both original and resolved
old candidate paths are protected using the accepted source-closure metadata.
The old full consumer and original outcomes are unchanged.

The first implementation and its preflight are preserved separately: independent
review found a resolved-path isolation defect, which was repaired.24 first-version
focused tests passed once;5 additional final-version synthetic integration tests
passed once in18.33s. These are software tests, not physical experiments.
The final actual baseline-reuse preflight passed:126 old terminals,2 pending,
zero new physical chains consumed. Scoped independent review receipt SHA
`eae86a7c645b858cb1ceaca0f3039cc04c34d4fe8abd06e298a1e6745657773e`.
Actual new last2 intake remains NOT_RUN: no new closed native publication.
This fixed126 reader is not a deployed persistent automatic consumer; future
single-then-second intake must preserve the already-consumed increment identity.

## Frozen64 and historical qualification

The research-side64 metadata interface was implemented and exercised once on
the actual unchanged proposal manifest:14 original analytic failures and50
unobserved eligible proposals.32 new synthetic tests passed; no original QA or
candidate preparation was rerun. The CLI only exposes `validate-context` and
cannot accept user-supplied claims of verified physical results. Its future pure
comparison helper is not yet a source-chain validator or deployed consumer.
Real-result ingestion remains NOT_INSTALLED pending the sole owner's exact
physical/birth interface. Solver starts, equal-budget coverage and equal-first4
qualified-train comparison are NOT_RUN/UNKNOWN, not inferred from reservations.

P215's existing215785-row audit index already binds source-row ordinal,10D
geometry,S4P and port-ground evidence; an existing SQLite identity index is also
located. Their old exact receipts/header were reused, not the full input tables
rescanned. Current GDS/process/Calibre/SRF/core qualifications are not encoded
in that39-column index. Different hash namespaces,111/56 grids and old/current
port/extraction semantics cannot be merged by naming or abs(k).
This removes a redundant index-rebuild step; it does not certify a partition.
Locator receipt SHA
`1ad75d16d3940c0d4236e447f528fc310e3d451188576657730a216f9fca0480`.
The31 research members remain distinct from production accepted; new submissions0,
new certified production uniques0, current certified100K total UNKNOWN.

## Native observation boundary

Sole-owner original-configuration SSH attempt at2026-09-11T00:40:39Z returned255:
`Connection closed by UNKNOWN port 65535`; no remote observation JSON was received.
An existing local SSH process is not proof of an authenticated usable MARS shell.
The last2 live status, current disk free/quota and allocated bytes per qualified
sample remain UNKNOWN. No new native controller, security bypass, extra cleanup,
model retraining or substitute-Q dispatch occurred in this research increment.
At the owner's01:09:16UTC checkpoint, the frozen64 GDS/EMX context and feature
bindings and a standalone native-process observer were implemented locally;
33 new tests passed (18 synthetic process/accounting,15 metadata/route).
Prior25 component tests were reused. Budgeted native-launch integration and
the owner loop/RESULT producer are still not implemented. New64 solve fails
closed, preventing fallback into the old unbudgeted execution route.
No Linux-native observer run, deployment, GDS, S4P or actual solver start occurred.
Owner checkpoint receipt SHA
`f17464d6fed69e5b2cf53f34b141c9985afe1f047e9ef37fd5b984663c7e679d`.
Two narrow offline findings (CPU-counter reset and persisted global-slot
validation) were sent to the owner; these do not authorize physical dispatch.

All figures remain author-made/verified under the current paper requirement.
This delivery provides source-backed numeric tables and code, not generated art.
