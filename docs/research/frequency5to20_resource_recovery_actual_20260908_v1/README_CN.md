# 资源等待恢复已实际安装：2026-09-08 19:43–19:44 UTC快照

MARS物理队列和本机增量报告器已实际恢复运行。本页只记录已有收据中的特定时间观察，不是实时连接，也不证明阅读本页时进程仍在运行。科研验收边界仍是63请求；新运营计数不能替代科学/视觉GO。

## 实际发生了什么

- MARS通过一次标准at提交（job2）启动原dispatcher的资源等待恢复入口。19:39:03 UTC从实际 `/proc` 核实PID **1050236**、start_ticks **370023765** 和精确10项argv；不是把exec前意图当成启动证明。
- 19:43:41 UTC，native PID仍在；12 GHz第五请求的Q16、Q18、Q19、Q20共有 **4个实际求解器**。旧11 GHz第五请求Q17的资源恢复侧收据文件已出现；这里仅确认记录存在，不据此发布新的误差或物理验收结论。
- 19:44:24 UTC，本机报告器PID **1978**被观察到运行，复用了 **72个**已捕获请求及原previous_figures，未重画基线。首轮只读检查的运营计数为 **73/320**，并有 **1份**新snapshot收据；该新快照尚无独立科学/视觉GO。观察时没有报告器FINAL。

准确时间、来源原SHA、启动身份和收据映射见[ACTUAL_INSTALLATION.json](ACTUAL_INSTALLATION.json)。所有PID都是带时间戳的记录，不是持续存活保证。

## 恢复范围没有改变研究合同

固定范围仍为 **320请求、每请求Q10–20共11槽、原3520候选槽**。保留原模型、目标帧、几何、失败记录和完成工件；资源恢复只处理原有、明确记录的求解前资源等待拒绝，不把已完成EMX重新求解。

MARS部署前已有 **106项原生环境合成测试PASS**，以及 **320请求输入preflight PASS**；这些是软件/输入验收，不是106或320份真实物理结果。精确源码、输入、启动配置及实际启动身份均有独立限定范围QA。

预算沿用原配置：最多 **4个并行solver × 每solver 2 CPU线程**。Native新任务准入截止为 **2026-09-10 18:00 UTC**，报告截止为 **2026-09-10 20:00 UTC**；这是现有工程预算，不是导师会议时间。已准入solver的完成等待遵循原程序，不由本包新增超时或信号逻辑。原queue/global锁与原报告锁保留；没有改生产、GUI或原数据规则。

本机已记录绑定报告器的有限闲置睡眠抑制。**开机/崩溃后自动重启为NOT_INSTALLED**；不得把一次at/Popen启动说成具备无限重试或掉线自愈。普通程序负责其有限状态检查，不由AI持续轮询代替。

## 已验收科研材料仍停在63请求

[累计63科学报告与51张已批准物理图](../frequency5to20_new23_cumulative63_20260908_v1/README_CN.md) · [累计63导师PPT与问答](../frequency5to20_advisor_cumulative63_20260908_v1/README_CN.md) · [10页PPT](../frequency5to20_advisor_cumulative63_20260908_v1/advisor_frequency5to20_cumulative63_v1.pptx)

已验收snapshot_0054为63请求、693原槽、560独立求解、533strict有效、27strict无效。仅7请求具备原11槽全strict的完整q_emx条件；失败和未完成项不得当成成功子集或总体准确率。已有51图与PPT63的精确GO不覆盖运营73或更新快照。

全部16频率模型仍为预算训练 **PARTIAL/PROVISIONAL**，不是充分收敛。15 GHz物理路线仍是 **DEVELOPMENT5K**；正式 **FORMAL10K的15 GHz fresh EMX仍NOT_RUN**，不混用。SELF_PROXY不是物理真值，有限目标帧上的描述性结果不证明因果冠军或部署总体精度。

PPT63保留其18:36停止观察，属于当时历史；本包追加19:43–19:44恢复后的新观察，不覆盖旧失败。PPT的10页、10原生图、6原生表已获得精确限定GO，但没有原生PowerPoint打开验收，也没有因本次恢复重新修改PPT。

## 公开包及来源边界

本包恰好3文件：本说明、ACTUAL_INSTALLATION.json和SHA256SUMS。它是来源支撑的元数据摘录，**不是私有收据的逐字节副本**。JSON中的source SHA/bytes都指原始私有文件；本包文件的实际SHA单独列于SHA256SUMS，不混淆两者。

本机私有工件仅用研究根相对 `reports/...` 定位，远端自有研究路径仅用 `MARS_RESEARCH_ROOT/...` 标记。这些不是GitHub下载链接，私有原件未上传。本包不包含权重、原始数据、S4P/GDS、PDK/许可证内容、机器凭据或完整私有进程命令。后续新增结果仍须独立数值/图表验收后才能更新科研结论；本页不执行恢复、测试、统计或仿真。
