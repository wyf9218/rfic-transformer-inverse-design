# M29 前置供给判断：未部署配额改动

本说明只使用已经取得的 M29 截面和本地精确源码，没有再次读取 MARS 状态、旧事件窗口或结果正文。生产运行状态的时间仍为 2026-09-12 13:44:51 UTC，不是本说明的写入时间。

## 已验证的实际实现

- `prepare_successor.prepare` 将后继的工具容量明确写成 Cadence8 / Calibre8 / EMX48；配置不是简单继承一个外层可热改参数。`run_controlled_native.Owner.__init__` 只在构造时读配置。没有找到本路径受支持的活动配额热加载入口。
- `run_fixed48_native.Owner.run` 使用候选流水线深度64，不是EMX线程数；每个候选同步经过 Cadence、GDS审计、Calibre、EMX、结果导出，候选会持有自己的线程和磁盘认领直到结束。`Manager` 明确要求64/48，不能只改外层深度值。
- `ResourceHistory.limits` 将各工具自己的槽数、许可证、共享CPU和内存预算取最小值。各工具返回的 additional 不能相加当成互不影响的额外额度；实际每次发许可前再次重算。
- `Manager.acquire` 每2秒重试，通过共用 dispatch_lock 串行执行身份校验、完整预算树统计、计划确认和许可落盘。它没有按EMX/Calibre/Cadence排序的持久优先队列；但没有锁等待/持有时长或全部未准入原因记录，因此不能证明锁导致了多少秒空档或将一次有限等待说成永久饥饿。工具实际等待不占该锁；资源采样不等待该锁。

上述七份源码逐一匹配已部署 DEPLOYMENT 的SHA和bytes。两个本地旧镜像不匹配时第一次诊断正确失败；已切换到现有 endpoint_v2_successor_v1 精确镜像，原失败脚本和说明保留。没有修改旧镜像或生产源码。

## 用现有快照做的离线策略复验

执行 `analyze_local.py`，EXIT0。`REPLAY_RECEIPT.json` SHA-256 `a6290c0d822ffe71e1b3ec2a44daa60a8dfaf7d93b96d4e6306960ca19c97571`，11708 bytes。这是已存快照策略重放，不是新资源PASS，不是并发速度测试，也不是部署测试。

- 原策略精确重现 additional Cadence1 / Calibre8 / EMX23。M29 Calibre许可0仍有11个待准入，EMX既无许可也无等待intent；因此Calibre的8路硬容量没有在这个截面被占满，增加它无法解释当时未派发。
- 唯一反事实只将Cadence容量8改12，结果变成5 / 8 / 23。它允许更多上游竞争共享资源，但并未增加CPU余量或已经就绪的EMX工作；真实吞吐收益UNKNOWN。未采用、未部署，没有修改任何门禁。
- M29测得空闲CPU当量64.24498，扣系统4及7份尚未反映到测量中的双CPU Cadence预留14，余46.24498；按每工具2CPU仅容23份新增许可。即便立刻有48个EMX就绪，也不能据该截面准入48个。后续容量需由原控制器自己的新测量判断，不能沿用此数字为实时值。
- 已有63份候选磁盘认领。5GiB减实际1035649024、尚需预留4101545984，仅剩231514112 bytes，可再容3份64MiB投影认领，合计66；现深度64另限制新增。提高深度不能绕过此预算，也没有证据证明这两份额外候选会提高正式吞吐。预算不增、不重开当前窗口。

## 决定与下一合法边界

**本次不部署配额或优先级改动。** 证据不能支持把Cadence/Calibre盲调高作为确定性修复；也没有证据支持重建优先队列或提前终止健康批次。48是请求和EMX容量，不是当前已运行数量。

现有可核实切换点为 `BATCH_TERMINAL_AND_OWNER_EXIT`，还须用既有受控交接核验元数据子进程自然退出、完整账本、无新旧派发重叠。当前M29 batch4仍有135/256终态，未到该边界；没有为本判断冻结、kill、替换continuation或安装第二控制器。

共享锁内I/O是明确存在的潜在调度开销，但因果耗时仍UNKNOWN；只有未来真实证据表明某一受限工具持续满额、就绪队列受其限制或准入锁确实长期阻塞，才有依据为下一安全边界做对应单项升级。本次不为补证新增AI轮询或重复M28事件读取。原有服务器执行和预算检查继续，不等待此说明。
